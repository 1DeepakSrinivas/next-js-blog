"use client";

import React from "react";

const Category = () => {
  return (
    <div onClick={() => {}} className="p-4 rounded-lg shadow-md cursor-pointer bg-slate-600">
      Title
    </div>
  );
};

export default Category;
